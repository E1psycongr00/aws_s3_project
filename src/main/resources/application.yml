spring:
  servlet:
    multipart:
      max-file-size: 5MB  # ?? ? ?? ?? ?? ??
      max-request-size: 15MB # ???? ?? ?? ?? ??


cloud:
  aws:
    s3:
      bucket: ?? ??
      accessKey: ??? ?
      secretKey: ?? ?
      region: ap-northeast-2
